<%= form_with(model: room_member) do |form| %>
  <% if room_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_member.errors.count, "error") %> prohibited this room_member from being saved:</h2>

      <ul>
        <% room_member.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :room_id %>
    <%= form.number_field :room_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
